---
name: vue-weui-next-demo
description: |
  This skill generates WeUI prototype pages that align product, design, and development around the same component system. It produces three files: (1) a standalone HTML prototype using WeUI CSS for product/design preview, (2) a tech doc HTML with per-component screenshots embedded alongside HTML structure and Vue component code, generated by running scripts/screenshot.js with Puppeteer, and (3) a .vue file for developers to use directly. Every component block in the HTML must use a wrapper div with data-component="组件名" and data-vue="<MpXxx ... />" attributes so the screenshot script can identify and capture each block individually. Trigger phrases include: "用 vue-weui-next 写", "生成 WeUI 原型", "生成 WeUI 组件", "按照 WeUI 规范", "生成 Demo", "写一个 WeUI 页面", "出一个原型图", "@tencent/vue-weui-next".
---

# vue-weui-next 生成规范

## 输出文件（每次必须输出三份）

| 文件 | 用途 |
|------|------|
| `{页面名}.html` | 产品 / 设计 — 浏览器直接预览原型 |
| `{页面名}_tech.html` | 开发 — 运行截图脚本后自动生成，包含每个组件的截图 + HTML + Vue 对照 |
| `{页面名}.vue` | 开发 — 直接复制进 Vue 工程使用 |

生成完三份文件后，**必须提示用户运行截图脚本**：

```bash
# 安装依赖（首次）
npm install puppeteer

# 生成技术文档（含截图）
node ~/.codebuddy/skills/vue-weui-next-demo/scripts/screenshot.js {页面名}.html
```

---

## 一、HTML 原型规范

### 1. 样式引入

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>页面标题</title>
  <link rel="stylesheet" href="https://res.wx.qq.com/open/libs/weui/2.6.0/weui.min.css" />
</head>
<body>
  <!-- 页面内容 -->
</body>
</html>
```

### 2. 组件块标记（必须遵循）

每个组件必须用 `<div data-component="..." data-vue="...">` 包裹，截图脚本依赖这两个属性：

- `data-component`：组件中文名，用于技术文档的标题和截图文件名
- `data-vue`：对应的 Vue 组件完整写法（单行字符串）

```html
<!-- ✅ 正确：每个组件单独一个 data-component 块 -->
<div data-component="主按钮" data-vue='<MpButton type="primary">提交</MpButton>'>
  <a class="weui-btn weui-btn_primary">提交</a>
</div>

<div data-component="手机号输入框" data-vue='<MpInput v-model="phone" type="phone" placeholder="请输入手机号" />'>
  <div class="weui-cell weui-cell_input">
    <div class="weui-cell__hd"><label class="weui-label">手机号</label></div>
    <div class="weui-cell__bd">
      <input class="weui-input" type="tel" placeholder="请输入手机号" />
    </div>
  </div>
</div>

<!-- ❌ 错误：多个组件混在一个 data-component 块里 -->
<div data-component="表单">
  <a class="weui-btn weui-btn_primary">提交</a>
  <div class="weui-cell weui-cell_input">...</div>
</div>
```

### 3. 完整页面结构模板

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>登录页</title>
  <link rel="stylesheet" href="https://res.wx.qq.com/open/libs/weui/2.6.0/weui.min.css" />
</head>
<body>
<div class="page">

  <div class="weui-cells__title">账号信息</div>
  <div class="weui-cells">

    <div data-component="用户名输入框" data-vue='<MpInput v-model="username" placeholder="请输入用户名" />'>
      <div class="weui-cell weui-cell_input">
        <div class="weui-cell__hd"><label class="weui-label">用户名</label></div>
        <div class="weui-cell__bd">
          <input class="weui-input" type="text" placeholder="请输入用户名" />
        </div>
      </div>
    </div>

    <div data-component="密码输入框" data-vue='<MpInput v-model="password" type="password" placeholder="请输入密码" />'>
      <div class="weui-cell weui-cell_input">
        <div class="weui-cell__hd"><label class="weui-label">密码</label></div>
        <div class="weui-cell__bd">
          <input class="weui-input" type="password" placeholder="请输入密码" />
        </div>
      </div>
    </div>

  </div>

  <div class="weui-btn-area">
    <div data-component="登录按钮" data-vue='<MpButton type="primary" @click="onLogin">登录</MpButton>'>
      <a class="weui-btn weui-btn_primary">登录</a>
    </div>
  </div>

</div>
</body>
</html>
```

---

## 二、Vue 文件规范（{页面名}.vue）

### 全量引入前提

项目 `main.ts` 必须全量引入，`.vue` 文件中**无需**单独 import 组件：

```ts
import { createApp } from 'vue'
import WeUI from '@tencent/vue-weui-next'
import App from './App.vue'
import '@tencent/vue-weui-next/dist/index.css'

createApp(App).use(WeUI).mount('#app')
```

### v-model 规范

```vue
<MpInput v-model="name" />          <!-- ✅ -->
<MpDialog v-model="visible" />      <!-- ✅ -->
<MpToast v-model:show="show" />     <!-- ✅ -->
<MpInput :value="name" @input="…" /> <!-- ❌ -->
```

### Script Setup 模板

```vue
<script setup lang="ts">
import { ref, reactive } from 'vue'
const form = reactive({ username: '', password: '' })
const dialogVisible = ref(false)
</script>
```

### 常见错误规避

| 错误写法 | 正确写法 |
|---------|---------|
| 在 .vue 中单独 import MpButton | 全量引入后无需 import |
| 忘记引入 CSS | main.ts 中加 `import '@tencent/vue-weui-next/dist/index.css'` |
| `<script setup>` 不加 `lang="ts"` | 加上 `lang="ts"` |
| Dialog 事件用 `@confirm` | 应使用 `@ok` |

---

## 参考资料

- HTML ↔ Vue 映射表：`references/html_vue_mapping.md`
- 完整组件 API：`references/api_reference.md`
- 工程模板代码：`references/project_template.md`
- 截图脚本：`scripts/screenshot.js`
- 官方文档：https://vue-weui-next.pages.woa.com/docs/guide/quickstart.html
